import React from "react";
import { COLORS } from "../../design.js";

// Placeholder — full implementation lands in Phase 4.
export default function FitChart() {
  return (
    <div style={{ padding: "48px 0", color: COLORS.muted, fontStyle: "italic" }}>
      Fit chart — coming in Phase 4.
    </div>
  );
}
